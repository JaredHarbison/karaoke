# frozen_string_literal: true

# Canonical provider-backed song identity shared by future event performances.
# Transitional constant for code that still names the canonical identity
# explicitly; Song is the domain model going forward.
class SongIdentity < Song
end
