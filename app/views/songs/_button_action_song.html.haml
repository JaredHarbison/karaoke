- if action == "finish"
  = link_to song.url, class: "song-action song-action--play", title: action, target: "_blank", data: { controller: "song-action", song_action_action_value: action, song_action_id_value: song.id, action: "click->song-action#perform" } do
    %span 
      = render "components/icons/play"
- elsif action == "skip"
  %button{ class: "song-action song-action--skip", title: song.skipped ? "resume" : "skip", data: { controller: "song-action", song_action_action_value: action, song_action_id_value: song.id, action: "click->song-action#perform" } }
    - if song.skipped 
      %span 
        = render "components/icons/add"
    - else 
      %span 
        = render "components/icons/skip"
- elsif action == "edit"
  = link_to edit_venue_song_path(Current.venue.slug, song), class: "song-action song-action--edit", title: action do
    %span
      = render "components/icons/edit"
- else 
  = button_to venue_song_path(Current.venue.slug, song), method: :delete, data: { confirm: 'Are you sure?' }, class: "song-action song-action--remove", title: action, form: { data: { turbo: false } } do
    %span 
      = render "components/icons/destroy"
