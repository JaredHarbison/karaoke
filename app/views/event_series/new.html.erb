<main class="management-page" aria-labelledby="new-event-series-heading">
  <%= render 'shared/management_page_header', heading_id: 'new-event-series-heading', title: 'Create Recurring Series', back_label: 'Back to Recurring Series', back_path: venue_event_series_index_path(Current.venue.slug) %>
  <section class="management-card management-form-card">
    <%= render "form", form_url: venue_event_series_index_path(Current.venue.slug), form_method: :post %>
  </section>
</main>
