<main class="container" aria-labelledby="new-event-series-heading">
  <p><%= link_to "Back to series", venue_event_series_index_path(Current.venue.slug) %></p>
  <h1 id="new-event-series-heading">Create recurring series</h1>
  <%= render "form", form_url: venue_event_series_index_path(Current.venue.slug), form_method: :post %>
</main>
