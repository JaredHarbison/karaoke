<main class="management-page" aria-labelledby="edit-event-series-heading">
  <%= render 'shared/management_page_header', heading_id: 'edit-event-series-heading', title: 'Edit Recurring Series', back_label: 'Back to Recurring Series', back_path: venue_event_series_index_path(Current.venue.slug) %>
  <section class="management-card management-form-card">
    <%= render "form", form_url: venue_event_series_path(Current.venue.slug, @event_series), form_method: :patch %>
  </section>
</main>
